apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: immich
  namespace: tailscale
spec:
  parentRefs:
  - name: main
    namespace: envoy-system
    sectionName: https
  hostnames:
    - "immich.nstnr.com"
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: immich-clusterip
          port: 80
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
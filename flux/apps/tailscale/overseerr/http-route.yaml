apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: overseerr
  namespace: tailscale
spec:
  parentRefs:
  - name: main
    namespace: envoy-system
    sectionName: https
  hostnames:
    - "overseerr.nstnr.com"
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: overseerr-clusterip
          port: 80
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /